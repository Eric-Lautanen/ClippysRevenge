{"id": "25d3b564-3532-4b69-88c6-9c880b3989a1", "schema_variant": "good_code", "source": "opensource", "category_id": "bbfed16fd381", "category": "Advanced - Zero-Copy Protocol Buffers", "difficulty": "advanced", "prompt": "I made this little thing that reads a bunch of text files, but I want it to handle really big logs without using too much memory. How can I make sure the reading part is super efficient when I process data from many different sources?", "code": "use std::fs::write;\nuse std::io::{self, BufReader, Result};\n\nstruct LogEntry {\n    source_file: String,\n    line_content: String,\n    timestamp: String,\n}\n\nfn process_log_stream(paths: &[&str]) -> io::Result<Vec<LogEntry>> {\n    let mut entries = Vec::new();\n    for path in paths {\n        println!(\"Processing file: {}\", path);\n        let file = std::fs::File::open(path)?;\n        let reader = BufReader::new(file);\n        for line_result in io::BufRead::lines(reader) {\n            let line = match line_result {\n                Ok(l) => l,\n                Err(e) => { \n                    eprintln!(\"Error reading line from {}: {}\", path, e);\n                    continue;\n                }\n            };\n            if !line.is_empty() {\n                let entry = LogEntry {\n                    source_file: path.to_string(),\n                    line_content: line,\n                    timestamp: \"2024-01-01T00:00:00Z\".to_string(),\n                };\n                entries.push(entry);\n            }\n        }\n    }\n    Ok(entries)\n}\n\nfn main() -> io::Result<()> {\n    let log_paths = &[\"test1.log\", \"test2.log\"];\n    for path in log_paths.iter() {\n        if !std::path::Path::new(path).exists() {\n            println!(\"Creating dummy file: {}\", path);\n            write(path, \"Some test line 1nAnother entry for n\")?;\n        }\n    }\n\n    match process_log_stream(log_paths) {\n        Ok(entries) => {\n            println!(\"Successfully aggregated {} log entries.\", entries.len());\n        },\n        Err(e) => eprintln!(\"An overall error occurred: {}\", e),\n    }\n    Ok(())\n}", "explanation": "The code uses `BufReader::lines` which reads file contents line-by-line, avoiding loading entire files into memory. This is the idiomatic way to stream text data from multiple sources in Rust without excessive overhead.", "concepts": ["File I/O", "Buffering", "Error Handling"], "crates": [], "edition": "2021", "source_model": "gemma-4-e4b-it", "validation": {"fmt": true, "clippy": false, "build": true, "test": false, "clippy_lints": [], "clippy_output": "Checking rust-example v0.1.0 (C:\\Users\\ericl\\AppData\\Local\\Temp\\frontier_kiwgz4q9\\rust-example)\nerror: fields `source_file`, `line_content`, and `timestamp` are never read\n --> src\\main.rs:5:5\n  |\n4 | struct LogEntry {\n  |        -------- fields in this struct\n5 |     source_file: String,\n  |     ^^^^^^^^^^^\n6 |     line_content: String,\n  |     ^^^^^^^^^^^^\n7 |     timestamp: String,\n  |     ^^^^^^^^^\n  |\n  = note: `-D dead-code` implied by `-D warnings`\n  = help: to override `-D warnings` add `#[expect(dead_code)]` or `#[allow(dead_code)]`\n\nerror: could not compile `rust-example` (bin \"rust-example\") due to 1 previous error", "auto_fixed": true}, "verified_at": "1775761875", "compiler_ver": "rustc 1.94.1 (e408947bf 2026-03-25)", "min_rust_version": "1.56.0", "has_unsafe": false, "license": "Apache-2.0"}
