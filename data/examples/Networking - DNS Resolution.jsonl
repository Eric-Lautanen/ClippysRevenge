{"id": "aa359fc4-b2c2-41a9-85f6-b6b1bd312e13", "schema_variant": "good_code", "source": "opensource", "category_id": "429d5c10f848", "category": "Networking - DNS Resolution", "difficulty": "intermediate", "prompt": "I need a small program that takes a website name as text input, like 'google.com', and outputs the numerical address so my game can connect to it.", "code": "use std::net::ToSocketAddrs;\n\nfn main() {\n    let hostname = std::env::args().nth(1).unwrap_or_else(|| \"localhost\".to_string());\n\n    match hostname.to_socket_addrs() {\n        Ok(mut addresses) => {\n            if let Some(address) = addresses.next() {\n                println!(\"Successfully resolved address: {}\", address);\n            }\n        },\n        Err(e) => {\n            eprintln!(\"Failed to resolve address for {}: {}\", hostname, e);\n        }\n    }\n}", "explanation": "Using `to_socket_addrs` is the standard library way to perform DNS resolution. It handles the underlying network calls required to convert a human-readable name into machine-usable IP addresses.", "concepts": ["DNS Resolution", "Error Handling"], "crates": [], "edition": "2021", "source_model": "gemma-4-e4b-it", "validation": {"fmt": true, "clippy": true, "build": true, "test": false, "clippy_lints": [], "clippy_output": "", "auto_fixed": true}, "verified_at": "1775648338", "compiler_ver": "rustc 1.94.1 (e408947bf 2026-03-25)", "min_rust_version": "1.56.0", "has_unsafe": false, "license": "Apache-2.0"}
{"id": "b1f38c06-5e98-46ae-8839-3e550ab5b3c0", "schema_variant": "good_code", "source": "opensource", "category_id": "429d5c10f848", "category": "Networking - DNS Resolution", "difficulty": "intermediate", "prompt": "So this webhook thing keeps failing when it tries to hit a URL, right? I need to write some logic that waits longer and longer between attempts until it finally succeeds or gives up.", "code": "use std::time::{Duration, Instant};\n\nstruct WebhookDispatcher {\n    max_retries: u32,\n    initial_delay: Duration,\n}\n\nimpl WebhookDispatcher {\n    fn new(max_retries: u32, initial_delay: Duration) -> Self {\n        WebhookDispatcher { max_retries, initial_delay }\n    }\n\n    async fn dispatch_webhook(&self, url: &str) -> Result<(), String> {\n        for attempt in 0..=self.max_retries {\n            if attempt > 0 {\n                let delay = self.calculate_backoff(attempt);\n                println!(\"Attempt {} failed. Waiting for {:?} before retrying...\", attempt + 1, delay);\n                tokio::time::sleep(delay).await;\n            }\n\n            match self.send_request(url).await {\n                Ok(_) => {\n                    println!(\"Successfully dispatched webhook to {}. Total attempts: {}\", url, attempt + 1);\n                    return Ok(())\n                },\n                Err(e) => {\n                    if attempt == self.max_retries {\n                        return Err(format!(\"Failed after {} attempts. Last error: {}\", self.max_retries + 1, e));\n                    }\n                }\n            }\n        }\n        Ok(())\n    }\n\n    async fn send_request(&self, url: &str) -> Result<(), String> {\n        println!(\"--- Sending request to {} ---\", url);\n        static mut ATTEMPTS_MADE: u32 = 0;\n        unsafe {\n            ATTEMPTS_MADE += 1;\n            if ATTEMPTS_MADE < 3 {\n                return Err(format!(\"Simulated connection failure for {}\", url));\n            }\n        }\n        Ok(())\n    }\n\n    fn calculate_backoff(&self, attempt: u32) -> Duration {\n        let exponential_factor = 2u32.pow(attempt - 1);\n        self.initial_delay.checked_mul(exponential_factor).unwrap()\n    }\n}\n\n#[tokio::main]\nasync fn main() {\n    let dispatcher = WebhookDispatcher::new(5, Duration::from_millis(100));\n    match dispatcher.dispatch_webhook(\"https://api.example.com/webhooks\").await {\n        Ok(_) => {},\n        Err(e) => println!(\"FATAL ERROR: {}\", e),\n    }\n}", "explanation": "The code uses an explicit loop structure and `std::thread::sleep` to implement exponential backoff logic. Since the core simulation function `send_request` is async, the main dispatch function must also be marked as `async`, necessitating the use of a runtime like `tokio` for compilation.", "concepts": ["Error Handling", "Time Management", "Asynchronous Programming"], "crates": ["tokio"], "edition": "2021", "source_model": "gemma-4-e4b-it", "validation": {"fmt": true, "clippy": true, "build": true, "test": false, "clippy_lints": [], "clippy_output": "", "auto_fixed": true}, "verified_at": "1775740623", "compiler_ver": "rustc 1.94.1 (e408947bf 2026-03-25)", "min_rust_version": "1.56.0", "has_unsafe": true, "license": "Apache-2.0"}
