{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I want to pass a Rust-allocated String to a C function that will free it. Using mem::forget prevents Rust from double-freeing the memory.","broken_code":"use std::ffi::CString;\n\nextern \"C\" { fn c_takes_ownership(s: *mut i8); }\n\nfn main() {\n    let s = CString::new(\"hello\").unwrap();\n    let ptr = s.as_ptr() as *mut i8;\n    unsafe { c_takes_ownership(ptr); }\n    // s drops here: Rust frees memory C already freed\n}","error_message":"","fixed_code":"use std::ffi::CString;\nuse std::mem;\n\nextern \"C\" { fn c_takes_ownership(s: *mut i8); }\n\nfn main() {\n    let s = CString::new(\"hello\").unwrap();\n    let ptr = s.into_raw(); // transfers ownership; CString will not drop\n    unsafe { c_takes_ownership(ptr); }\n    // C is responsible for freeing ptr\n    // If we needed mem::forget instead:\n    // let ptr = s.as_ptr() as *mut i8;\n    // mem::forget(s);\n    // unsafe { c_takes_ownership(ptr); }\n}","explanation":"`CString::into_raw()` is the idiomatic way to transfer ownership to C: it consumes the `CString` (running no destructor) and returns the raw pointer. `mem::forget(s)` is the manual equivalent: it consumes `s` without running its `Drop` impl, preventing a double-free when C frees the pointer. Never use `as_ptr()` without `forget` or `into_raw` when transferring ownership — the `CString` would still be dropped at the end of scope.","concepts":["mem-forget","CString","FFI","ownership-transfer","double-free-prevention"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504000","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I wrap a value in ManuallyDrop to delay its destruction until I explicitly call drop. Show the basic pattern.","broken_code":"use std::mem::ManuallyDrop;\n\nstruct Bomb;\nimpl Drop for Bomb {\n    fn drop(&mut self) { println!(\"BOOM\"); }\n}\n\nfn main() {\n    let b = Bomb;\n    println!(\"before scope end\");\n    // want: drop manually at a chosen point, not at scope end\n}","error_message":"","fixed_code":"use std::mem::ManuallyDrop;\n\nstruct Bomb;\nimpl Drop for Bomb {\n    fn drop(&mut self) { println!(\"BOOM\"); }\n}\n\nfn main() {\n    let mut b = ManuallyDrop::new(Bomb);\n    println!(\"before explicit drop\");\n    unsafe { ManuallyDrop::drop(&mut b); } // explicit drop\n    println!(\"after explicit drop\");\n    // b goes out of scope here but Drop is NOT called again\n}","explanation":"`ManuallyDrop<T>` wraps a value and inhibits its automatic destructor. The value is still allocated (on the stack or heap) but its `Drop` impl will not run when the `ManuallyDrop` goes out of scope. To run the destructor you call `ManuallyDrop::drop(&mut md)` explicitly in an `unsafe` block — unsafe because you must ensure it is called at most once and that the value is not used afterward.","concepts":["ManuallyDrop","explicit-drop","defer-destructor","unsafe","Drop"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504001","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I implement a custom Vec-like type and need to drop only the initialized elements, not the entire buffer. Show using ManuallyDrop for the buffer.","broken_code":"struct MyVec<T> {\n    ptr: *mut T,\n    len: usize,\n    cap: usize,\n}\n// If T has Drop, the memory is freed but individual elements are not dropped","error_message":"","fixed_code":"use std::mem::ManuallyDrop;\n\nstruct MyVec<T> {\n    buf: ManuallyDrop<Vec<T>>,\n    len: usize,\n}\n\nimpl<T> MyVec<T> {\n    fn new() -> Self {\n        MyVec { buf: ManuallyDrop::new(Vec::new()), len: 0 }\n    }\n    fn push(&mut self, val: T) {\n        self.buf.push(val);\n        self.len += 1;\n    }\n}\n\nimpl<T> Drop for MyVec<T> {\n    fn drop(&mut self) {\n        // Drop only the live elements, then free the buffer\n        unsafe {\n            // truncate to len (already = buf.len() here, but illustrative)\n            self.buf.set_len(0); // prevent Vec from dropping elements\n            ManuallyDrop::drop(&mut self.buf); // free the allocation\n        }\n    }\n}\n\nfn main() {\n    let mut v: MyVec<String> = MyVec::new();\n    v.push(String::from(\"hello\"));\n    v.push(String::from(\"world\"));\n    println!(\"len: {}\", v.len);\n}","explanation":"Custom collection types often hold their buffer in `ManuallyDrop<Vec<T>>` to take precise control of when elements are dropped vs when the allocation is freed. The `Drop` impl drops elements explicitly (e.g., by calling destructors on each slot in `ptr..ptr+len`), then frees the allocation. Without `ManuallyDrop`, the inner `Vec`'s own `Drop` would run automatically, possibly double-dropping elements.","concepts":["ManuallyDrop","custom-collection","drop-order","buffer-management","unsafe"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504002","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I try to use mem::forget on a MutexGuard to intentionally leak the lock (poison the mutex). Show the pattern and the safer alternative.","broken_code":"use std::sync::Mutex;\n\nfn main() {\n    let m = Mutex::new(42i32);\n    let guard = m.lock().unwrap();\n    std::mem::forget(guard); // leak the lock: mutex stays locked forever\n    // Any subsequent lock() on m will block forever\n    println!(\"mutex is now permanently locked\");\n}","error_message":"","fixed_code":"use std::sync::{Mutex, MutexGuard};\nuse std::mem;\n\n// Intentional lock leak for testing poisoning behaviour:\nfn leak_lock<'a, T>(guard: MutexGuard<'a, T>) {\n    mem::forget(guard);\n}\n\nfn main() {\n    let m = Mutex::new(42i32);\n    {\n        let guard = m.lock().unwrap();\n        println!(\"got lock: {}\", *guard);\n        // Normally guard drops here, releasing the lock\n    }\n    // Demonstrate mem::forget effect on a separate mutex:\n    let m2 = Mutex::new(0i32);\n    let g2 = m2.lock().unwrap();\n    mem::forget(g2); // g2's Drop (which releases the lock) never runs\n    // m2 is now permanently locked; trying to lock again deadlocks\n    println!(\"m2 is leaked\");\n}","explanation":"`mem::forget` consumes a value without running its `Drop` impl. For a `MutexGuard`, this means the lock is never released — a permanent leak. This is occasionally intentional in tests or when building lock-free transitions, but must be avoided in production. The safe alternative is always to let guards drop naturally via scope, or use explicit `drop(guard)`.","concepts":["mem-forget","MutexGuard","lock-leak","intentional-leak","Drop"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504003","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":false,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I use ManuallyDrop to read a value out of a field without triggering the field's Drop, then drop the rest of the struct normally.","broken_code":"use std::mem::ManuallyDrop;\n\nstruct Pair {\n    name: String,\n    value: i32,\n}\n\nimpl Drop for Pair {\n    fn drop(&mut self) {\n        println!(\"dropping Pair\");\n    }\n}\n\nfn extract_name(p: Pair) -> String {\n    // want to move p.name out without running Pair::drop\n    p.name // error: cannot move out of type that implements Drop\n}","error_message":"error[E0509]: cannot move out of type `Pair`, which implements the `Drop` trait\n --> src/main.rs:17:5\n  |\n17|     p.name\n  |     ^^^^^^ cannot move out of here","error_code":"E0509","fixed_code":"use std::mem::ManuallyDrop;\n\nstruct Pair {\n    name: String,\n    value: i32,\n}\n\nfn extract_name(p: Pair) -> String {\n    let mut md = ManuallyDrop::new(p);\n    // Safety: we take name out and never use md again\n    let name = unsafe { ManuallyDrop::take(&mut md) }.name;\n    // md goes out of scope without Drop running on the Pair\n    // name is moved out safely\n    // Note: the String in name.value is intentionally leaked here\n    // In real code you would also handle dropping `value` manually\n    name\n}\n\nfn main() {\n    let p = Pair { name: String::from(\"alice\"), value: 42 };\n    let n = extract_name(p);\n    println!(\"{}\", n);\n}","explanation":"You cannot move a field out of a type that implements `Drop` (E0509) because Rust cannot guarantee the type's `Drop` impl won't use that field. `ManuallyDrop::new(p)` inhibits the destructor. `ManuallyDrop::take` gives back the inner value without running Drop. You are then responsible for dropping any remaining fields you don't need (here `value: i32` which is `Copy` so no action needed).","concepts":["ManuallyDrop","E0509","field-extraction","Drop-impl","take"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504004","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I receive a raw pointer from C and want to wrap it in a Rust struct that will free it on drop, without any double-free.","broken_code":"extern \"C\" {\n    fn c_alloc() -> *mut u8;\n    fn c_free(ptr: *mut u8);\n}\n\nfn main() {\n    let ptr = unsafe { c_alloc() };\n    // If we just return, ptr leaks.\n    // If we Box::from_raw it, Rust uses the wrong allocator to free it.\n    unsafe { c_free(ptr); }\n}","error_message":"","fixed_code":"use std::mem::ManuallyDrop;\n\nextern \"C\" {\n    fn c_alloc() -> *mut u8;\n    fn c_free(ptr: *mut u8);\n}\n\nstruct CBuffer {\n    ptr: ManuallyDrop<*mut u8>,\n}\n\nimpl CBuffer {\n    unsafe fn new() -> Self {\n        CBuffer { ptr: ManuallyDrop::new(c_alloc()) }\n    }\n    fn as_ptr(&self) -> *mut u8 { *self.ptr }\n}\n\nimpl Drop for CBuffer {\n    fn drop(&mut self) {\n        unsafe { c_free(*self.ptr); }\n    }\n}\n\nfn main() {\n    let buf = unsafe { CBuffer::new() };\n    println!(\"ptr: {:p}\", buf.as_ptr());\n    // buf drops here, calling c_free exactly once\n}","explanation":"Wrapping a C-allocated pointer in a struct with a custom `Drop` that calls `c_free` is the RAII pattern for FFI resources. We store the raw pointer in `ManuallyDrop<*mut u8>` to make it explicit that we control the destruction. Rust must not use its allocator to free a C-allocated pointer — only the matching `c_free` must be used. The struct's `Drop` calls exactly that.","concepts":["ManuallyDrop","FFI","RAII","c-free","custom-drop"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504005","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I try to use mem::forget to skip cleanup during a panic unwind. Show why this is dangerous and the correct approach with catch_unwind or proper RAII.","broken_code":"use std::mem;\n\nfn risky() {\n    let resource = String::from(\"resource\");\n    might_panic();\n    mem::forget(resource); // intended to skip cleanup, but panic bypasses this line\n}\n\nfn might_panic() {\n    panic!(\"oops\");\n}\n\nfn main() {\n    let _ = std::panic::catch_unwind(risky);\n}","error_message":"","fixed_code":"struct Resource(String);\n\nimpl Drop for Resource {\n    fn drop(&mut self) {\n        println!(\"cleaning up: {}\", self.0);\n    }\n}\n\nfn risky() {\n    let _resource = Resource(String::from(\"resource\"));\n    might_panic(); // if this panics, Drop runs automatically\n    // No need for explicit cleanup — RAII handles it\n}\n\nfn might_panic() {\n    panic!(\"oops\");\n}\n\nfn main() {\n    let _ = std::panic::catch_unwind(risky);\n    println!(\"caught panic\");\n}","explanation":"`mem::forget` is not run during a panic unwind — the panic bypasses the rest of the function and only runs `Drop` impls. Relying on `forget` for cleanup-skipping is therefore ineffective and dangerous. The correct approach is RAII: implement `Drop` on a wrapper struct. The destructor runs whether the function returns normally or via panic, guaranteeing cleanup in all paths.","concepts":["mem-forget","panic","RAII","unwind","drop-on-panic"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504006","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":false,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I use ManuallyDrop in a union to hold types with destructors. Show why this is required.","broken_code":"union MyUnion {\n    i: i32,\n    s: String, // error: union fields with non-Copy types require ManuallyDrop\n}\n\nfn main() {\n    let u = MyUnion { i: 42 };\n    println!(\"{}\", unsafe { u.i });\n}","error_message":"error[E0740]: field must implement `Copy` or be wrapped in `ManuallyDrop<...>` to be used in a union\n --> src/main.rs:3:5\n  |\n3 |     s: String,\n  |     ^^^^^^^^^","error_code":"E0740","fixed_code":"use std::mem::ManuallyDrop;\n\nunion MyUnion {\n    i: i32,\n    s: ManuallyDrop<String>,\n}\n\nfn main() {\n    let u = MyUnion { i: 42 };\n    println!(\"int: {}\", unsafe { u.i });\n\n    let mut u2 = MyUnion { s: ManuallyDrop::new(String::from(\"hello\")) };\n    // Must manually drop the String variant when done\n    unsafe { ManuallyDrop::drop(&mut u2.s); }\n}","explanation":"Rust requires union fields with non-`Copy` types to be wrapped in `ManuallyDrop`. This is because the compiler cannot know which variant of the union is active, so it cannot automatically call the correct destructor. `ManuallyDrop` signals \"I take responsibility for calling Drop\". You must track which variant is active and call `ManuallyDrop::drop` on the correct field at the right time.","concepts":["ManuallyDrop","union","E0740","non-Copy-union","manual-destructor"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504007","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I build a slab allocator where slots are ManuallyDrop<T> so I control when each slot's destructor runs.","broken_code":"struct Slab<T> {\n    slots: Vec<Option<T>>,\n}\n// Problem: when Vec drops, Option<T> drops each T automatically,\n// even slots we consider \"freed\" by our allocator logic.","error_message":"","fixed_code":"use std::mem::ManuallyDrop;\n\nstruct Slab<T> {\n    slots: Vec<ManuallyDrop<T>>,\n    occupied: Vec<bool>,\n}\n\nimpl<T> Slab<T> {\n    fn with_capacity(cap: usize) -> Self {\n        Slab {\n            slots: (0..cap).map(|_| ManuallyDrop::new(unsafe { std::mem::zeroed() })).collect(),\n            occupied: vec![false; cap],\n        }\n    }\n    unsafe fn insert(&mut self, idx: usize, val: T) {\n        self.slots[idx] = ManuallyDrop::new(val);\n        self.occupied[idx] = true;\n    }\n    unsafe fn remove(&mut self, idx: usize) -> T {\n        self.occupied[idx] = false;\n        ManuallyDrop::take(&mut self.slots[idx])\n    }\n}\n\nimpl<T> Drop for Slab<T> {\n    fn drop(&mut self) {\n        for (i, occ) in self.occupied.iter().enumerate() {\n            if *occ {\n                unsafe { ManuallyDrop::drop(&mut self.slots[i]); }\n            }\n        }\n    }\n}\n\nfn main() {\n    let mut slab: Slab<String> = Slab::with_capacity(4);\n    unsafe {\n        slab.insert(0, String::from(\"hello\"));\n        slab.insert(2, String::from(\"world\"));\n        let s = slab.remove(0);\n        println!(\"removed: {}\", s);\n    }\n}","explanation":"In a slab allocator, slots may hold uninitialised or previously-freed values. Using `ManuallyDrop<T>` prevents the `Vec` from automatically dropping every element when the slab is destroyed. The custom `Drop` impl iterates only over occupied slots and explicitly drops them. This pattern is fundamental to implementing arena allocators, object pools, and other custom memory managers.","concepts":["ManuallyDrop","slab-allocator","custom-allocator","drop-control","unsafe"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504008","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I want to convert a Vec<T> into a Vec<U> reusing the same allocation when T and U have the same size and alignment. ManuallyDrop prevents double-free.","broken_code":"fn vec_cast<T, U>(v: Vec<T>) -> Vec<U> {\n    // naive: would require allocation\n    v.into_iter().map(|x| unsafe {\n        std::mem::transmute(x)\n    }).collect()\n}","error_message":"","fixed_code":"use std::mem::{self, ManuallyDrop};\n\nunsafe fn vec_cast<T, U>(v: Vec<T>) -> Vec<U> {\n    assert_eq!(mem::size_of::<T>(), mem::size_of::<U>());\n    assert_eq!(mem::align_of::<T>(), mem::align_of::<U>());\n    let mut md = ManuallyDrop::new(v);\n    // Reconstruct as Vec<U> using the same raw parts\n    Vec::from_raw_parts(\n        md.as_mut_ptr() as *mut U,\n        md.len(),\n        md.capacity(),\n    )\n}\n\nfn main() {\n    let v: Vec<u32> = vec![1, 2, 3];\n    let w: Vec<i32> = unsafe { vec_cast(v) };\n    println!(\"{:?}\", w);\n}","explanation":"`ManuallyDrop::new(v)` inhibits `v`'s destructor. We then reconstruct a `Vec<U>` from the same raw pointer, length, and capacity via `Vec::from_raw_parts`. The new `Vec<U>` owns the allocation and will free it when it drops. Without `ManuallyDrop`, the original `Vec<T>` would also try to free the same allocation, causing a double-free. The size/alignment assertions are required for soundness.","concepts":["ManuallyDrop","vec-cast","reuse-allocation","from-raw-parts","transmute"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504009","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I demonstrate that mem::forget is safe (not marked unsafe) and explain why — it is the responsibility of the type implementor to handle forgetting.","broken_code":"use std::mem;\n\nstruct File(std::fs::File);\n\nimpl Drop for File {\n    fn drop(&mut self) { println!(\"file closed\"); }\n}\n\nfn main() {\n    let f = File(std::fs::File::open(\"Cargo.toml\").unwrap());\n    mem::forget(f); // safe to call! But the file descriptor leaks.\n}","error_message":"","fixed_code":"use std::mem;\n\nstruct LeakProof<T>(T);\n\nimpl<T> Drop for LeakProof<T> {\n    fn drop(&mut self) {\n        println!(\"LeakProof dropped\");\n    }\n}\n\nfn main() {\n    // mem::forget is SAFE — it is always valid to not run a destructor.\n    // Rust's safety guarantee is: no undefined behaviour from forgetting.\n    // Forgetting may leak resources (file handles, memory) but is not UB.\n    let x = LeakProof(42i32);\n    mem::forget(x); // safe; the i32 leaks but no UB occurs\n    println!(\"x was forgotten, no drop message printed\");\n\n    // Contrast with ManuallyDrop::drop which IS unsafe:\n    let mut md = std::mem::ManuallyDrop::new(String::from(\"hello\"));\n    unsafe { std::mem::ManuallyDrop::drop(&mut md); } // unsafe: must only call once\n}","explanation":"`mem::forget` is safe because Rust's memory model does not require destructors to run. Types that rely on their `Drop` impl running (like `MutexGuard`) can be broken by `forget`, but no undefined behaviour results — just resource leaks. This is why `unsafe` is not needed. `ManuallyDrop::drop`, however, is `unsafe` because calling it twice is a double-free.","concepts":["mem-forget","safe-not-unsafe","leak-is-safe","Drop-not-guaranteed","ManuallyDrop-drop-unsafe"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504010","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I implement a into_parts method on a custom struct that has a Drop impl, using ManuallyDrop to extract fields without triggering the destructor.","broken_code":"struct Buffer {\n    data: Vec<u8>,\n    len: usize,\n}\n\nimpl Drop for Buffer {\n    fn drop(&mut self) {\n        println!(\"Buffer dropped, {} bytes freed\", self.len);\n    }\n}\n\nimpl Buffer {\n    fn into_parts(self) -> (Vec<u8>, usize) {\n        (self.data, self.len) // E0509: can't move out of Drop type\n    }\n}","error_message":"error[E0509]: cannot move out of type `Buffer`, which implements the `Drop` trait","error_code":"E0509","fixed_code":"use std::mem::ManuallyDrop;\n\nstruct Buffer {\n    data: Vec<u8>,\n    len: usize,\n}\n\nimpl Drop for Buffer {\n    fn drop(&mut self) {\n        println!(\"Buffer dropped, {} bytes freed\", self.len);\n    }\n}\n\nimpl Buffer {\n    fn into_parts(self) -> (Vec<u8>, usize) {\n        let mut md = ManuallyDrop::new(self);\n        let data = unsafe { ManuallyDrop::take(&mut md) }.data;\n        // len is Copy so we can read it before take\n        // Actually take consumes the whole thing; use ptr::read for individual fields:\n        let len = md.len;\n        let data = unsafe { std::ptr::read(&md.data) };\n        (data, len)\n    }\n}\n\nfn main() {\n    let buf = Buffer { data: vec![1, 2, 3], len: 3 };\n    let (data, len) = buf.into_parts();\n    println!(\"{:?} {}\", data, len);\n}","explanation":"E0509 prevents moving fields out of a type that implements `Drop`. Wrap `self` in `ManuallyDrop::new(self)` to suppress the destructor, then use `std::ptr::read` to bitwise-copy each field out. After `ptr::read`, the original bits in `md` are logically unowned — the destructor won't run because `ManuallyDrop` inhibits it. This is the canonical `into_parts` pattern for Drop types.","concepts":["ManuallyDrop","E0509","into-parts","ptr-read","field-extraction"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504011","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I build a scope guard that runs cleanup on drop, and use mem::forget to defuse it when cleanup is not needed.","broken_code":"struct Guard<F: FnOnce()> {\n    f: Option<F>,\n}\n\nimpl<F: FnOnce()> Guard<F> {\n    fn new(f: F) -> Self { Guard { f: Some(f) } }\n}\n\nimpl<F: FnOnce()> Drop for Guard<F> {\n    fn drop(&mut self) {\n        if let Some(f) = self.f.take() { f(); }\n    }\n}\n\nfn main() {\n    let _g = Guard::new(|| println!(\"cleanup!\"));\n    // want to cancel cleanup but there's no defuse method\n}","error_message":"","fixed_code":"use std::mem;\n\nstruct Guard<F: FnOnce()> {\n    f: Option<F>,\n}\n\nimpl<F: FnOnce()> Guard<F> {\n    fn new(f: F) -> Self { Guard { f: Some(f) } }\n    fn defuse(mut self) {\n        self.f = None; // clear the closure so Drop is a no-op\n        // alternatively: mem::forget(self);\n    }\n}\n\nimpl<F: FnOnce()> Drop for Guard<F> {\n    fn drop(&mut self) {\n        if let Some(f) = self.f.take() { f(); }\n    }\n}\n\nfn main() {\n    let g = Guard::new(|| println!(\"cleanup!\"));\n    println!(\"doing work\");\n    g.defuse(); // cancel cleanup\n    println!(\"work done, no cleanup\");\n\n    let g2 = Guard::new(|| println!(\"cleanup2!\"));\n    mem::forget(g2); // also cancels — but defuse is clearer\n}","explanation":"A scope guard pattern runs cleanup in `Drop`. Two ways to defuse: (1) set the inner `Option<F>` to `None` so `Drop` is a no-op, or (2) call `mem::forget(guard)` to prevent `Drop` from running. Option 1 is cleaner because the intent is explicit. `mem::forget` also works but is less self-documenting. The `defuse` method is the idiomatic interface.","concepts":["mem-forget","scope-guard","defuse","Drop-cancel","RAII"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504012","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":false,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I use ManuallyDrop to construct an uninitialized array of non-Copy types for a fixed-size stack.","broken_code":"fn main() {\n    // Cannot create [MaybeUninit<String>; 4] and then use it without ManuallyDrop\n    let arr: [String; 4] = [String::new(), String::new(), String::new(), String::new()];\n    // All 4 Strings are immediately dropped at end of scope even if we only used 2\n}","error_message":"","fixed_code":"use std::mem::{ManuallyDrop, MaybeUninit};\n\nstruct FixedStack<T, const N: usize> {\n    data: [ManuallyDrop<T>; N],\n    len: usize,\n}\n\nimpl<T, const N: usize> FixedStack<T, N> {\n    fn new() -> Self {\n        FixedStack {\n            data: unsafe { MaybeUninit::uninit().assume_init() },\n            len: 0,\n        }\n    }\n    fn push(&mut self, val: T) -> bool {\n        if self.len >= N { return false; }\n        self.data[self.len] = ManuallyDrop::new(val);\n        self.len += 1;\n        true\n    }\n    fn pop(&mut self) -> Option<T> {\n        if self.len == 0 { return None; }\n        self.len -= 1;\n        Some(unsafe { ManuallyDrop::take(&mut self.data[self.len]) })\n    }\n}\n\nimpl<T, const N: usize> Drop for FixedStack<T, N> {\n    fn drop(&mut self) {\n        for i in 0..self.len {\n            unsafe { ManuallyDrop::drop(&mut self.data[i]); }\n        }\n    }\n}\n\nfn main() {\n    let mut stack: FixedStack<String, 4> = FixedStack::new();\n    stack.push(String::from(\"a\"));\n    stack.push(String::from(\"b\"));\n    println!(\"popped: {:?}\", stack.pop());\n}","explanation":"`[ManuallyDrop<T>; N]` stores N slots without automatically dropping any of them. The custom `Drop` impl drops only the `len` initialized slots. Using `MaybeUninit::uninit().assume_init()` creates the array without initializing the `ManuallyDrop` wrappers — safe because `ManuallyDrop` has no validity requirements beyond its contained type being validly initialized before access.","concepts":["ManuallyDrop","MaybeUninit","fixed-array","custom-stack","partial-init"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504013","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I demonstrate that forgetting a value that owns heap memory leaks the memory but does not cause undefined behaviour.","broken_code":"use std::mem;\n\nfn main() {\n    for _ in 0..1_000_000 {\n        let big = vec![0u8; 1024];\n        mem::forget(big); // leaks 1KB per iteration!\n    }\n    println!(\"done\"); // process uses ~1GB of leaked memory\n}","error_message":"","fixed_code":"use std::mem;\n\nfn main() {\n    // Demonstration: single controlled forget\n    let v = vec![1u8, 2, 3];\n    let ptr = v.as_ptr();\n    let len = v.len();\n    mem::forget(v); // v is gone; the heap allocation persists until process exit\n    println!(\"leaked {} bytes at {:p}\", len, ptr);\n    // Safe but bad practice outside specific FFI or unsafe patterns\n\n    // Correct pattern: just let values drop\n    let v2 = vec![1u8, 2, 3];\n    println!(\"v2 will be properly freed\");\n    drop(v2); // explicit drop; same as end of scope\n}","explanation":"`mem::forget` on a heap-allocated value leaks the allocation — the memory is not freed until the process exits. This is defined behaviour in Rust (safe code can always leak). But leaking in a loop causes unbounded memory growth. The correct mental model: `forget` is a tool for specific patterns (FFI ownership transfer, defusing guards) not a general drop-suppressor. Always prefer letting values drop naturally.","concepts":["mem-forget","memory-leak","defined-behaviour","heap-allocation","drop-vs-forget"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504014","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":false,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I use ManuallyDrop to split a struct with a Drop impl into its raw components for an unsafe transmutation.","broken_code":"use std::mem;\n\nstruct Wrapper(Vec<u8>);\nimpl Drop for Wrapper {\n    fn drop(&mut self) { println!(\"wrapper dropped\"); }\n}\n\nfn main() {\n    let w = Wrapper(vec![1, 2, 3]);\n    // Cannot transmute a type that implements Drop\n    let v: Vec<u8> = unsafe { mem::transmute(w) }; // ERROR\n    println!(\"{:?}\", v);\n}","error_message":"error[E0512]: cannot transmute between types of different sizes, or dependently-sized types\n...","error_code":"E0512","fixed_code":"use std::mem::{self, ManuallyDrop};\n\nstruct Wrapper(Vec<u8>);\nimpl Drop for Wrapper {\n    fn drop(&mut self) { println!(\"wrapper dropped\"); }\n}\n\nfn main() {\n    let w = Wrapper(vec![1, 2, 3]);\n    let md = ManuallyDrop::new(w);\n    // Safety: Wrapper is repr(transparent) equivalent to Vec<u8>\n    // We must not drop md; the Vec<u8> will own the allocation.\n    let v: Vec<u8> = unsafe { mem::transmute::<ManuallyDrop<Wrapper>, Vec<u8>>(md) };\n    println!(\"{:?}\", v);\n}","explanation":"You cannot `transmute` a type that implements `Drop` directly — the compiler rejects it. Wrapping in `ManuallyDrop` suppresses the `Drop` impl, making the transmute valid. After the transmute, `ManuallyDrop<Wrapper>` no longer exists; the `Vec<u8>` owns the allocation. This pattern requires `Wrapper` and `Vec<u8>` to have the same memory layout (here guaranteed by the single-field newtype).","concepts":["ManuallyDrop","transmute","Drop-prevents-transmute","newtype","layout-equivalence"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504015","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I implement a GC-like system where objects have ManuallyDrop wrappers and a separate collector calls drop at the right time.","broken_code":"use std::mem::ManuallyDrop;\n\nstruct GcObject<T> {\n    value: ManuallyDrop<T>,\n    alive: bool,\n}\n\nimpl<T> GcObject<T> {\n    fn new(val: T) -> Self {\n        GcObject { value: ManuallyDrop::new(val), alive: true }\n    }\n}\n\nstruct Gc<T> {\n    objects: Vec<GcObject<T>>,\n}\n\nimpl<T> Gc<T> {\n    fn alloc(&mut self, val: T) -> usize {\n        self.objects.push(GcObject::new(val));\n        self.objects.len() - 1\n    }\n    fn collect(&mut self) {\n        for obj in self.objects.iter_mut().filter(|o| !o.alive) {\n            unsafe { ManuallyDrop::drop(&mut obj.value); }\n        }\n        self.objects.retain(|o| o.alive);\n    }\n}\n\nimpl<T> Drop for Gc<T> {\n    fn drop(&mut self) {\n        for obj in self.objects.iter_mut().filter(|o| o.alive) {\n            unsafe { ManuallyDrop::drop(&mut obj.value); }\n        }\n    }\n}\n\nfn main() {\n    let mut gc: Gc<String> = Gc { objects: Vec::new() };\n    let idx = gc.alloc(String::from(\"hello\"));\n    gc.objects[idx].alive = false;\n    gc.collect();\n    println!(\"collected\");\n}","error_message":"","fixed_code":"use std::mem::ManuallyDrop;\n\nstruct GcObject<T> {\n    value: ManuallyDrop<T>,\n    alive: bool,\n}\n\nimpl<T> GcObject<T> {\n    fn new(val: T) -> Self {\n        GcObject { value: ManuallyDrop::new(val), alive: true }\n    }\n}\n\nstruct Gc<T> {\n    objects: Vec<GcObject<T>>,\n}\n\nimpl<T> Gc<T> {\n    fn new() -> Self { Gc { objects: Vec::new() } }\n    fn alloc(&mut self, val: T) -> usize {\n        self.objects.push(GcObject::new(val));\n        self.objects.len() - 1\n    }\n    fn mark_dead(&mut self, idx: usize) {\n        self.objects[idx].alive = false;\n    }\n    fn collect(&mut self) {\n        for obj in self.objects.iter_mut().filter(|o| !o.alive) {\n            unsafe { ManuallyDrop::drop(&mut obj.value); }\n        }\n        self.objects.retain(|o| o.alive);\n    }\n}\n\nimpl<T> Drop for Gc<T> {\n    fn drop(&mut self) {\n        for obj in self.objects.iter_mut().filter(|o| o.alive) {\n            unsafe { ManuallyDrop::drop(&mut obj.value); }\n        }\n    }\n}\n\nfn main() {\n    let mut gc: Gc<String> = Gc::new();\n    let idx = gc.alloc(String::from(\"hello\"));\n    gc.alloc(String::from(\"world\"));\n    gc.mark_dead(idx);\n    gc.collect();\n    println!(\"collected dead objects\");\n}","explanation":"A manual GC stores objects in `ManuallyDrop<T>` wrappers so Rust's automatic destructors don't interfere. The collector calls `ManuallyDrop::drop` only on dead objects. The `Gc`'s own `Drop` impl calls it on remaining live objects when the GC itself is destroyed. This pattern is the foundation of arena allocators and language runtime memory managers.","concepts":["ManuallyDrop","GC","arena","drop-control","collector"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504016","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I use mem::forget to suppress the destructor of a type that registers/deregisters itself in a global registry, allowing the entry to persist.","broken_code":"use std::sync::Mutex;\n\nstatic REGISTRY: Mutex<Vec<String>> = Mutex::new(Vec::new());\n\nstruct Entry(String);\nimpl Entry {\n    fn new(name: &str) -> Self {\n        REGISTRY.lock().unwrap().push(name.to_owned());\n        Entry(name.to_owned())\n    }\n}\nimpl Drop for Entry {\n    fn drop(&mut self) {\n        REGISTRY.lock().unwrap().retain(|n| n != &self.0);\n    }\n}\n\nfn main() {\n    let e = Entry::new(\"sensor\");\n    std::mem::forget(e); // entry persists in registry after scope\n    println!(\"{:?}\", REGISTRY.lock().unwrap()); // [\"sensor\"]\n}","error_message":"","fixed_code":"use std::sync::Mutex;\n\nstatic REGISTRY: Mutex<Vec<String>> = Mutex::new(Vec::new());\n\nstruct Entry(String);\nimpl Entry {\n    fn new(name: &str) -> Self {\n        REGISTRY.lock().unwrap().push(name.to_owned());\n        Entry(name.to_owned())\n    }\n    fn leak(self) {\n        std::mem::forget(self); // prevent deregistration\n    }\n}\nimpl Drop for Entry {\n    fn drop(&mut self) {\n        REGISTRY.lock().unwrap().retain(|n| n != &self.0);\n    }\n}\n\nfn main() {\n    let e = Entry::new(\"sensor\");\n    e.leak(); // deliberate: entry stays in registry forever\n    println!(\"registry: {:?}\", REGISTRY.lock().unwrap());\n}","explanation":"`mem::forget` (wrapped in a `leak` method) intentionally prevents the `Drop` impl from deregistering the entry. This is a legitimate pattern for static/permanent registrations (plugins, signal handlers, service discovery). The `leak` method name makes the intent explicit and Clippy-friendly. The registry retains the entry for the process lifetime.","concepts":["mem-forget","registry","intentional-leak","static-registration","Drop-suppression"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504017","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":false,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I use ManuallyDrop::new and ManuallyDrop::into_inner to conditionally run or skip a destructor.","broken_code":"use std::mem::ManuallyDrop;\n\nstruct Expensive(Vec<u8>);\nimpl Drop for Expensive {\n    fn drop(&mut self) { println!(\"expensive drop\"); }\n}\n\nfn maybe_drop(val: Expensive, should_drop: bool) {\n    if should_drop {\n        drop(val); // runs Drop\n    } else {\n        std::mem::forget(val); // suppresses Drop\n    }\n}","error_message":"","fixed_code":"use std::mem::ManuallyDrop;\n\nstruct Expensive(Vec<u8>);\nimpl Drop for Expensive {\n    fn drop(&mut self) { println!(\"expensive drop\"); }\n}\n\nfn maybe_drop(val: Expensive, should_drop: bool) {\n    let md = ManuallyDrop::new(val);\n    if should_drop {\n        // Reconstruct the value and let it drop normally\n        let _ = ManuallyDrop::into_inner(md);\n    }\n    // else: md goes out of scope, no Drop runs\n}\n\nfn main() {\n    maybe_drop(Expensive(vec![1, 2, 3]), true);  // prints \"expensive drop\"\n    maybe_drop(Expensive(vec![4, 5, 6]), false); // silent\n}","explanation":"`ManuallyDrop::into_inner(md)` extracts the inner value, giving it back its normal ownership — including its `Drop` impl. Letting the result fall out of scope (via `let _ = …`) runs the destructor. If `should_drop` is false, `md` exits scope without `into_inner` being called, so the `Drop` is skipped. This is cleaner than the `forget`-in-else branch approach.","concepts":["ManuallyDrop","into-inner","conditional-drop","Drop-control","pattern"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504018","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":false,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I need to return a raw pointer to Rust-allocated data to C, then later reconstruct and free it. Show the full round-trip.","broken_code":"fn allocate_for_c() -> *mut String {\n    let s = Box::new(String::from(\"owned by C now\"));\n    Box::into_raw(s) // correct! Box::into_raw transfers ownership\n}\n\nfn free_from_c(ptr: *mut String) {\n    unsafe { drop(Box::from_raw(ptr)); } // reconstruct Box to free\n}","error_message":"","fixed_code":"use std::mem;\n\n/// Allocate a String on Rust's heap and hand the raw pointer to C.\n/// C must call `free_string` when done.\n#[no_mangle]\npub extern \"C\" fn make_string() -> *mut String {\n    let s = Box::new(String::from(\"hello from Rust\"));\n    Box::into_raw(s)\n}\n\n/// Reconstruct the Box and drop it, freeing the String.\n/// Safety: ptr must have been produced by `make_string` and not yet freed.\n#[no_mangle]\npub unsafe extern \"C\" fn free_string(ptr: *mut String) {\n    if !ptr.is_null() {\n        drop(Box::from_raw(ptr));\n    }\n}\n\nfn main() {\n    // Simulate C calling both functions\n    let ptr = make_string();\n    unsafe {\n        println!(\"C sees: {:?}\", &*ptr);\n        free_string(ptr);\n    }\n    println!(\"freed\");\n}","explanation":"`Box::into_raw` is the idiomatic `forget`-equivalent for `Box<T>`: it consumes the Box without dropping, yielding a raw pointer. `Box::from_raw` is the inverse: it reconstructs the Box (and thus the Drop machinery) from the raw pointer. This is the standard Rust→C ownership transfer pattern. Using `mem::forget` manually is equivalent but less idiomatic than `into_raw`.","concepts":["Box-into-raw","Box-from-raw","FFI-ownership","mem-forget-equivalent","no-mangle"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504019","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I show that ManuallyDrop<T> has the same size and alignment as T, making it suitable for unsafe layout tricks.","broken_code":"use std::mem;\nuse std::mem::ManuallyDrop;\n\nfn main() {\n    // Is ManuallyDrop<T> the same size as T?\n    println!(\"{}\", mem::size_of::<ManuallyDrop<String>>()); // ??\n    println!(\"{}\", mem::size_of::<String>());\n}","error_message":"","fixed_code":"use std::mem;\nuse std::mem::ManuallyDrop;\n\nfn main() {\n    assert_eq!(mem::size_of::<ManuallyDrop<String>>(), mem::size_of::<String>());\n    assert_eq!(mem::align_of::<ManuallyDrop<String>>(), mem::align_of::<String>());\n    assert_eq!(mem::size_of::<ManuallyDrop<Vec<u8>>>(), mem::size_of::<Vec<u8>>());\n    println!(\"ManuallyDrop<T> has same size as T: {} bytes\", mem::size_of::<ManuallyDrop<String>>());\n\n    // Safe to transmute between T and ManuallyDrop<T>\n    let s = String::from(\"hello\");\n    let md: ManuallyDrop<String> = ManuallyDrop::new(s);\n    let s_back: String = ManuallyDrop::into_inner(md);\n    println!(\"{}\", s_back);\n}","explanation":"`ManuallyDrop<T>` is guaranteed by the language to be a zero-overhead wrapper: it has the same size, alignment, and ABI as `T`. This makes it safe to transmute between `T` and `ManuallyDrop<T>` and to use them interchangeably in `repr(C)` structs and unions. This zero-cost property is what makes `ManuallyDrop` suitable for performance-critical unsafe code.","concepts":["ManuallyDrop","size-of","align-of","zero-overhead","layout-guarantee"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504020","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":false,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I use mem::forget in an intrusive linked-list implementation to prevent double-free when nodes are removed.","broken_code":"struct Node {\n    value: i32,\n    next: *mut Node,\n}\n\nstruct List {\n    head: *mut Node,\n}\n\nimpl List {\n    fn push(&mut self, val: i32) {\n        let node = Box::new(Node { value: val, next: self.head });\n        self.head = Box::into_raw(node);\n    }\n    fn pop(&mut self) -> Option<i32> {\n        if self.head.is_null() { return None; }\n        unsafe {\n            let node = Box::from_raw(self.head); // takes ownership\n            self.head = node.next;\n            Some(node.value)\n        } // node drops here: correct, Box frees the memory\n    }\n}\n\nimpl Drop for List {\n    fn drop(&mut self) {\n        while self.pop().is_some() {} // pop frees each node\n    }\n}\n\nfn main() {\n    let mut list = List { head: std::ptr::null_mut() };\n    list.push(1);\n    list.push(2);\n    println!(\"{:?}\", list.pop());\n}","error_message":"","fixed_code":"struct Node {\n    value: i32,\n    next: *mut Node,\n}\n\nstruct List {\n    head: *mut Node,\n}\n\nimpl List {\n    fn new() -> Self { List { head: std::ptr::null_mut() } }\n    fn push(&mut self, val: i32) {\n        let node = Box::new(Node { value: val, next: self.head });\n        self.head = Box::into_raw(node);\n    }\n    fn pop(&mut self) -> Option<i32> {\n        if self.head.is_null() { return None; }\n        unsafe {\n            let node = Box::from_raw(self.head);\n            self.head = node.next;\n            Some(node.value)\n        }\n    }\n}\n\nimpl Drop for List {\n    fn drop(&mut self) {\n        while self.pop().is_some() {}\n    }\n}\n\nfn main() {\n    let mut list = List::new();\n    list.push(1);\n    list.push(2);\n    list.push(3);\n    while let Some(v) = list.pop() {\n        println!(\"{}\", v);\n    }\n}","explanation":"The code is already correct: `Box::into_raw` when pushing (`mem::forget` equivalent — transfers allocation ownership to the raw pointer) and `Box::from_raw` when popping (reconstructs the `Box` which then drops the node). The list's `Drop` calls `pop` until empty, ensuring every node is freed exactly once. This pattern uses `into_raw`/`from_raw` instead of `forget` for clarity.","concepts":["Box-into-raw","mem-forget-equivalent","intrusive-list","double-free-prevention","raw-pointer"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504021","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I try to call ManuallyDrop::drop twice on the same value, causing undefined behaviour. Show why it is unsafe and how to prevent it.","broken_code":"use std::mem::ManuallyDrop;\n\nfn main() {\n    let mut md = ManuallyDrop::new(String::from(\"hello\"));\n    unsafe {\n        ManuallyDrop::drop(&mut md); // first drop: ok\n        ManuallyDrop::drop(&mut md); // second drop: use-after-free!\n    }\n}","error_message":"","fixed_code":"use std::mem::ManuallyDrop;\n\nstruct OwnedMd<T> {\n    inner: ManuallyDrop<T>,\n    dropped: bool,\n}\n\nimpl<T> OwnedMd<T> {\n    fn new(val: T) -> Self {\n        OwnedMd { inner: ManuallyDrop::new(val), dropped: false }\n    }\n    fn drop_once(&mut self) {\n        if !self.dropped {\n            self.dropped = true;\n            unsafe { ManuallyDrop::drop(&mut self.inner); }\n        }\n    }\n}\n\nimpl<T> Drop for OwnedMd<T> {\n    fn drop(&mut self) {\n        self.drop_once(); // safe: idempotent\n    }\n}\n\nfn main() {\n    let mut owned = OwnedMd::new(String::from(\"hello\"));\n    owned.drop_once(); // explicit first drop\n    owned.drop_once(); // no-op: already dropped\n    println!(\"no double-free\");\n}","explanation":"`ManuallyDrop::drop` is `unsafe` precisely because calling it more than once is undefined behaviour (use-after-free). The `OwnedMd` wrapper adds a `dropped: bool` flag to make drops idempotent — the standard technique for preventing double-frees in unsafe code. This is the same pattern used in FFI wrappers and custom smart pointers.","concepts":["ManuallyDrop","double-free","use-after-free","idempotent-drop","unsafe-invariant"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504022","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I use mem::ManuallyDrop as a field in a repr(C) struct for FFI interoperability with a C struct that has a destructor.","broken_code":"#[repr(C)]\nstruct CppObject {\n    data: String, // String has Rust ABI, not C ABI; also its Drop conflicts\n}\n\nextern \"C\" {\n    fn destroy_cpp_object(obj: *mut CppObject);\n}","error_message":"","fixed_code":"use std::mem::ManuallyDrop;\nuse std::os::raw::c_int;\n\n/// Mirrors a C struct: { int id; char* name; }\n#[repr(C)]\nstruct CRecord {\n    id: c_int,\n    name: ManuallyDrop<*mut std::os::raw::c_char>,\n}\n\nextern \"C\" {\n    fn free_record(r: *mut CRecord);\n}\n\nimpl CRecord {\n    fn get_id(&self) -> i32 { self.id }\n}\n\nimpl Drop for CRecord {\n    fn drop(&mut self) {\n        unsafe { free_record(self as *mut CRecord); }\n    }\n}\n\nfn main() {\n    println!(\"CRecord size: {} bytes\", std::mem::size_of::<CRecord>());\n}","explanation":"In `repr(C)` structs for FFI, fields must have predictable C-compatible layout. Using `ManuallyDrop<*mut c_char>` for a pointer field makes the destructor entirely manual — the `CRecord::Drop` calls the C destructor function instead. This ensures Rust doesn't call a Rust destructor on a C-owned pointer, and the C destructor is called exactly once via the custom `Drop` impl.","concepts":["ManuallyDrop","repr-C","FFI-struct","C-destructor","ABI"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504023","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}
{"category":"Core - ManuallyDrop and Forget","difficulty":"advanced","prompt":"I use mem::forget to implement a simple reference-count increment without a smart pointer, then show why ManuallyDrop is cleaner.","broken_code":"use std::rc::Rc;\nuse std::mem;\n\nfn clone_raw(rc: &Rc<i32>) -> *const i32 {\n    let cloned = Rc::clone(rc);\n    let ptr = Rc::as_ptr(&cloned);\n    mem::forget(cloned); // prevent decrement; raw ptr now \"owns\" a ref count\n    ptr\n}\n\nunsafe fn release_raw(ptr: *const i32) {\n    // Reconstruct Rc to decrement count\n    let _ = Rc::from_raw(ptr);\n}","error_message":"","fixed_code":"use std::rc::Rc;\nuse std::mem;\n\n/// Produce a raw pointer that logically owns one Rc ref-count increment.\nfn into_raw_rc(rc: Rc<i32>) -> *const i32 {\n    Rc::into_raw(rc) // consumes rc (like forget) and returns raw ptr\n}\n\n/// Reconstruct the Rc, decrementing the count on drop.\nunsafe fn from_raw_rc(ptr: *const i32) -> Rc<i32> {\n    Rc::from_raw(ptr)\n}\n\nfn main() {\n    let rc = Rc::new(42);\n    let rc2 = Rc::clone(&rc);\n    let ptr = into_raw_rc(rc2); // rc2 consumed; count stays at 2\n    println!(\"count: {}\", Rc::strong_count(&rc)); // 2\n    let recovered = unsafe { from_raw_rc(ptr) };\n    println!(\"recovered: {}\", *recovered);\n    // recovered drops: count goes to 1\n}","explanation":"`Rc::into_raw` / `Rc::from_raw` are the idiomatic alternatives to `clone + forget + as_ptr`. They clearly communicate intent: ownership of one reference-count increment is transferred to/from a raw pointer. `mem::forget(cloned)` works but is harder to audit. The `into_raw`/`from_raw` pattern is used by FFI callbacks that need to keep Rust objects alive across C boundaries.","concepts":["mem-forget","Rc-into-raw","Rc-from-raw","reference-count","FFI-callback"],"crates":[],"edition":"2021","source_model":"claude-sonnet-4-6","validation":{"fmt":true,"clippy":true,"build":true,"test":true,"clippy_lints":[]},"verified_at":"1742504024","compiler_ver":"rustc 1.86.0","min_rust_version":"1.56.0","has_unsafe":true,"license":"Apache-2.0"}